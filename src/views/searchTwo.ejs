<!DOCTYPE html>
<html>
  <%- include('partials/head') %>
  <body>
    <%- include('partials/header') %>
    
      <% if(result.length <= 0) { %>

        <h3>No encontramos el personaje</h3>
  
        <%  }  else{ %>
  

      <% for(let i = 0; i < result.length; i++) {%>
        <div class="characterContainer">
    
          <div>
    
            <img src="/image/characters/<%= result[i].image %>" style="max-width: 300px;" alt="">
    
          </div>
    
          <div>
    
            <h2> <%= result[i].name %> </h2>
    
          </div>
            
          <button> <a href="/characters/<%= result[i].id %> ">Detalle</a> </button>
    
        </div>
        <% } %>

        <%  } %>
  </body>
</html>
